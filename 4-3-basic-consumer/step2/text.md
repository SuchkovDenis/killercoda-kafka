# Запускаем и читаем

Запустите consumer:

`python3 /root/consumer.py`{{exec}}

Видим 10 сообщений с ключами, значениями, партициями и офсетами.

Ключевые настройки:
- `group.id` — идентификатор группы консьюмеров
- `auto.offset.reset: earliest` — начать с самого начала (при первом запуске)
