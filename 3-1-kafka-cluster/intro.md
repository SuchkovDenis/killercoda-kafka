# Docker Compose + KRaft + Kafka UI

В этом сценарии мы развернём полноценный Kafka-кластер из трёх брокеров, работающий на протоколе KRaft (без ZooKeeper), и подключим к нему веб-интерфейс Kafka UI.

Docker уже установлен в этом окружении. Приступаем!
